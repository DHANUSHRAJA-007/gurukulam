name: gurukulam
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.2
  provider: ^6.1.5+1
  http: ^1.6.0
  intl: ^0.20.3
  printing: ^5.14.2
  dropdown_search: ^7.0.0
  equatable: ^2.0.5
  json_annotation: ^4.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  json_serializable: ^6.7.0

flutter:
  uses-material-design: true

  # ✅ CORRECT - Point to assets/image/
  assets:
    - assets/image/